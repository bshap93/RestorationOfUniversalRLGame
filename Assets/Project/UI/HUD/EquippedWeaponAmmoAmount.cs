using UnityEngine;

namespace Project.UI.HUD
{
    public class EquippedWeaponAmmoAmount : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
            gameObject.SetActive(false);
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
